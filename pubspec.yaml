name: cs490_stock_ticker
description: A new Flutter project.

version: 1.0.0+1

environment:
    sdk: '>=2.1.0 <3.0.0'

dependencies:
    flutter:
        sdk: flutter
    cupertino_icons: ^0.1.2
    http: ^0.12.0
    path:
    sqflite:
    pull_to_refresh:

dev_dependencies:
    flutter_test:
        sdk: flutter

flutter:
    uses-material-design: true
